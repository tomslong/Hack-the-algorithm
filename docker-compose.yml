version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DEBUG=False
      - SECRET_KEY=your-secret-key-change-in-production
      - HOST=0.0.0.0
      - PORT=5000
    volumes:
      - .:/app
    restart: unless-stopped
